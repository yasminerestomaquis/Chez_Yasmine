# Changelog — Chez Yasmine

## [Unreleased]

### Ajouté (2026-09-13) — Bénéfice net dans Graphiques, e-mail/statut de connexion dans Utilisateurs, bouton de synchronisation
- **Graphiques → Bénéfices (vue « Total »)** : tient désormais compte de toutes les natures de dépenses (Loyer, Salaires, Cie, Eau, Patentes, Entretien, Bouteilles de gaz, Charbon — pas seulement « Marché ») et des pertes enregistrées. Les dépenses à cycle long (Loyer/Cie/Eau : 30 jours, Patentes : 365 jours) sont réparties au prorata du recouvrement avec la période demandée plutôt que comptées en entier le jour du paiement (un loyer mensuel ne pèse plus que sa juste part sur une semaine) ; les autres natures et les pertes sont comptées à leur date réelle. Les vues par catégorie/produit restent en marge brute — aucune dépense générale n'est logiquement attribuable à un produit précis. Voir `docs/api/charts.md`.
- **Module Utilisateurs** : affiche désormais l'e-mail de chaque membre et son statut de connexion (point vert « En ligne », ou « Hors ligne · il y a X »/« Jamais connecté »), basé sur une activité API récente (moins de 2 minutes) plutôt qu'une vraie présence temps réel (pas de WebSocket dans ce projet à ce jour). Voir `docs/api/users.md`.
- **Accueil** : nouveau bouton de synchronisation/actualisation (icône 🔄), juste à gauche des Notifications — recharge la page pour prendre en compte les dernières données et un éventuel nouveau déploiement.

### Corrigé (2026-09-13) — Caisse/Tables : N° de commande/marché, paiement en un clic, accès Gérant
- `lastOrderNumber`/`lastMarketNumber` (suggestion pré-remplie du N° de commande/marché à l'encaissement) triaient par date de création plutôt que par le numéro lui-même — incohérent avec les suggestions de *nouveau* numéro (Achats/Dépenses), corrigé pour trier par le numéro décroissant partout. Voir `docs/api/pos.md`.
- Le dialogue de paiement de la Caisse/Tables n'a plus qu'un seul bouton, **« Valider le paiement »**, toujours actif — le bouton distinct « Ajouter la ligne de paiement » est retiré. Pour un paiement à un seul mode (l'écrasante majorité des cas), un clic suffit désormais au lieu de deux ; un paiement fractionné reste possible, le même bouton enregistrant la ligne courante et rouvrant le dialogue tant qu'un solde subsiste.
- Le rôle **Gérant** n'a plus accès au module Utilisateurs (permission `users.manage` retirée) — la tuile correspondante est aussi masquée de l'accueil pour ce rôle, réservé désormais à Super Administrateur/Administrateur/Propriétaire.
- 338/338 tests NestJS, `flutter analyze`/`test`/`build web` ✅ (64/64 tests Flutter).

### Ajouté (2026-09-12) — Refonte du module Dépenses (4 onglets) + sous-module Salaires/Paie
- Le module **Dépenses** est réorganisé en 4 sous-onglets : **Vue d'ensemble** (filtres Année/Mois/Semaine avec navigation précédent/suivant, 4 KPI avec évolution en % vs période précédente, graphique d'évolution, répartition par catégorie en donut, 10 dernières dépenses avec « Voir tout → » vers Historique), **Dépenses** (formulaire et liste existants, extraits verbatim — aucun champ retiré ou ajouté), **Salaires** (nouveau, voir ci-dessous) et **Historique** (nouveau : filtres Période/Nature/Statut/Mode de paiement, pagination, export Excel qui respecte strictement les filtres actifs).
- Nouveau sous-module **Salaires/Paie** : gestion des employés (nom, téléphone, date de naissance, salaire hebdomadaire ; jamais de suppression physique, seulement une mise en inactif), et un workflow de paie hebdomadaire **préparé → validé → payé → annulé**. Payer une paie crée automatiquement et de façon idempotente une dépense « Salaires » correspondante (transaction unique, contrainte `UNIQUE` en base en défense en profondeur contre un double paiement) — le montant de cette dépense n'est plus jamais saisissable manuellement. Nouvelles permissions `payroll.manage`/`payroll.view`, accordées uniquement à Administrateur/Comptable/Gérant/Propriétaire/Super Administrateur.
- Upload de photo employé **volontairement différé** : le pipeline d'upload existant cible un bucket Storage et des policies RLS pensés pour les images du catalogue produit, à ne pas réutiliser tel quel pour des photos RH sans audit dédié. Voir `docs/api/expenses.md` pour le détail complet (modèle de données, état machine, garanties d'idempotence, raisons de chaque déviation par rapport au plan initial).
- 322/322 tests NestJS, `flutter analyze`/`test`/`build web` ✅ (54/54 tests Flutter). Revue de code finale de bout en bout : aucune régression détectée sur les modules Rapports/Graphiques ni sur aucun autre module existant.

### Ajouté (2026-09-11) — Accès en lecture seule du Serveur à l'Historique Achats et au Stock
- Le Serveur peut désormais consulter l'onglet **Historique** d'Achats (commandes déjà enregistrées, fournisseur inclus) — sans « Créer une commande », « Liste de commandes », gestion des fournisseurs, ni modification/suppression d'une commande depuis son détail. Nouvelle permission `purchases.view` (lecture seule, séparée de `purchases.manage`).
- Le Serveur peut désormais consulter tout le module **Stock** (liste des produits, statut, historique par produit) — sans pouvoir créer de mouvement de stock, et sans la carte « Valeur du stock » (masquée). Nouvelle permission `stock.view` (lecture seule, séparée de `stock.manage`).
- `purchases.view`/`stock.view` accordées à Serveur et Magasinier ; seed rejoué sur la base de production (idempotent). Voir `docs/api/purchasing.md` et `docs/api/stock.md`.
- 299/299 tests NestJS, `flutter analyze`/`test`/`build web` ✅ (47/47 tests Flutter).

### Ajouté (2026-09-11) — Vue « boissons » de l'accueil pour le Serveur
- Le Serveur a reçu `reports.view` (nécessaire pour l'appel API sous-jacent) mais l'accueil (`HomeDashboard`) ne lui montre, à sa demande, que « Recettes boissons aujourd'hui », « Boissons · Espèces » et « Boissons · Mobile Money » — total « Ventes aujourd'hui », recettes/paiements Plats, Commandes aujourd'hui et Alertes stock restent masqués pour ce rôle (réservés aux rôles avec vue d'ensemble). Voir `docs/api/reports.md`.
- `flutter analyze`/`test`/`build web` ✅ (47/47 tests Flutter).

### Corrigé (2026-09-11) — Caisse/Tables inutilisables par Caissier et Serveur (`products.view`)
- Même après le correctif de l'accueil ci-dessous, un compte Serveur (ou Caissier) ne pouvait toujours pas ouvrir la Caisse ni ajouter un produit à une addition : `PosPage`/`TableOrderPage`/`FloorPlanPage` listent le catalogue via `GET .../products` et `GET .../categories`, routes qui exigeaient `products.manage` — une permission que **ni Caissier ni Serveur** n'ont (seuls Magasinier, Gérant et l'administration l'ont).
- Corrigé en séparant lecture et gestion : nouvelle permission `products.view` (lecture seule), affectée aux routes `GET` du catalogue (produits, catégories, URL d'image) à la place de `products.manage` (qui reste réservé à la création/édition/suppression). `products.view` accordée à Caissier, Serveur et Magasinier — voir `docs/api/catalog.md`.
- Seed rejoué sur la base de production (idempotent, `on conflict do nothing`) : 16 permissions (+1), 81 associations rôle/permission (+7).
- 299/299 tests NestJS, `flutter analyze`/`build web` inchangés (aucun changement côté Flutter, seulement des permissions serveur).

### Corrigé (2026-09-11) — Accueil bloqué pour les rôles sans `reports.view` (Serveur, Magasinier)
- Un compte invité avec le rôle Serveur voyait, à sa première connexion, l'écran d'accueil entier remplacé par « Impossible de joindre l'API : Permission(s) manquante(s) : reports.view » — alors qu'il a parfaitement le droit d'utiliser Tables/Caisse (`pos.sell`, `tables.manage`). Cause : `HomeDashboard._load()` groupait 3 appels réseau dans un seul `Future.wait`, dont 2 (`summary`/`paymentCategoryBreakdown`) exigent `reports.view` — une permission que Serveur et Magasinier n'ont pas (`supabase/seed/001_roles_permissions.sql`) — faisant échouer tout le chargement de la page au lieu de simplement ces 2 appels.
- Corrigé : ces 2 appels sont désormais tolérants à un 403 (autre erreur toujours fatale, comportement inchangé) ; l'accueil masque juste les cartes de statistiques concernées et garde la navigation (modules, actions rapides) intacte pour les rôles sans `reports.view`.
- `flutter analyze`/`test`/`build web` ✅ (47/47 tests Flutter).

### Ajouté (2026-09-11) — « Boissons vendues » (Rapports) : listing intégré + export Excel
- Nouveau bouton dans Rapports (icône 🍺, à côté du menu Export existant) : affiche, **directement dans l'application**, le listing des produits vendus des catégories Bières/Vins/Sucreries (`hasCasePricing`) pour une date choisie — colonnes Nom du produit / N° de commande / Quantité vendue / Montant total, ligne de total en gras. Construit côté client à partir de deux routes déjà déployées (`GET .../products`, `GET .../sales?day=`).
- Le dialogue propose aussi un bouton **« Exporter en Excel »**, qui télécharge le fichier `.xlsx` équivalent via `GET .../reports/beverages-sold.xlsx` (`exceljs`, nommé `Boissons vendues jj-mm-aaaa.xlsx`) — **les deux options coexistent** (décision utilisateur).
- **Détour technique en cours de route** : le premier push de la route Excel n'est jamais apparu sur Render (404 persistant plus de 15 minutes) — diagnostiqué comme un `package-lock.json` désynchronisé faisant échouer `npm ci` en CI **et** le build Docker Render (ancienne image conservée). Le listing intégré a été livré en attendant, sans dépendre de ce correctif ; le lockfile régénéré (`rm -rf node_modules package-lock.json && npm install`) a ensuite débloqué le déploiement Render, permettant de reconnecter le bouton d'export.
- 299/299 tests NestJS, `flutter analyze`/`test`/`build web` propres (47/47 tests Flutter). Vérifié en conditions réelles : `GET .../reports/beverages-sold.xlsx` répond désormais 401 (route en place) sur `chez-yasmine-api.onrender.com`.

### Ajouté (post-plan, 2026-09-10) — Multi-additions par table, libération sans condition, écran Caisse-table
- **Libération d'une table sans condition** (`POST /tables/:tableId/release`) : toutes ses additions ouvertes sont annulées (aucun impact vente/stock), la table redevient libre — sans confirmation, accessible depuis le nouveau menu d'une table occupée.
- **Plusieurs additions simultanées sur une même table** : nouveau bouton « Nouvelle addition » (`POST /tables/:tableId/additions`) en plus de la scission déjà existante ; `GET /tables/:tableId/orders` liste désormais toutes les additions ouvertes. Correction de deux bugs latents trouvés en chemin : le paiement d'une addition libérait systématiquement sa table même si une autre addition y restait ouverte, et `TablesService.list` n'agrégeait qu'une seule addition par table pour le total/nombre de convives affichés.
- **Écran d'addition refondu** (`table_order_page.dart`, remplace `order_detail_page.dart`) : reprend la grille produits + panier de la Caisse (composants extraits et partagés dans `lib/pos/`) au lieu de la liste plate/dialogue texte précédents, avec un onglet par addition. Les produits à prix variable (Poulets, Poissons, Plats africains) deviennent commandables depuis une table (saisie du prix, comme en Caisse) — jusqu'ici explicitement refusés.
- **Modifier/supprimer une table** rendu plus découvrable : nouvelle entrée « Modifier la table » dans le menu de chaque table (en plus de l'appui long déjà existant, conservé).
- Voir `docs/superpowers/specs/2026-09-10-table-order-caisse-design.md` pour le design complet.
- 296/296 tests NestJS, `flutter analyze`/`test`/`build web` propres (47/47 tests Flutter). Revue finale du plan : deux bugs Critiques trouvés et corrigés avant mise en production — `POST .../additions` ne renvoyait pas `items`, faisant planter le parsing Flutter (« Nouvelle addition » invisible malgré une création réussie côté serveur) ; les boutons +/- du panier n'étaient pas désactivés pendant une mutation en cours, un double-tap pouvant perdre un incrément de quantité.
- **Corrigé le même jour, en conditions réelles** : le panier en feuille modale (Caisse, mobile) ne se rafraîchissait pas visuellement sur +/- — bug préexistant, non lié à ce plan, signalé par l'utilisateur et corrigé (`lib/pos/pos_page.dart`).

### Ajouté (post-plan, 2026-09-10) — Sous-module Stock : sélection multiple, numérotation des lots par commande/marché, colonne Perdu, produits épuisés
- **Numérotation des lots ↔ commande/marché** (décision utilisateur) : un lot `L00N` correspond désormais exactement à la commande N°N (Bières/Vins/Sucreries) ou au marché N°N (Poulets/Poissons/Plats africains) qui l'a réellement produit, et ne s'affiche plus si cette commande/ce marché n'existe pas. `computeFifoLots` (`stock-lots.ts`) parse ce numéro depuis `StockMovement.reason` (déjà `"Commande n°X"` pour les achats) ; `ChartsService.stockLots` renumérote/masque en conséquence après vérification contre `Purchase.orderNumber`/`Expense.marketNumber` réels. Une entrée manuelle de stock sur une catégorie à prix variable exige désormais un **N° de marché** validé contre une dépense « Marché » déjà enregistrée (`CreateStockMovementDto.marketNumber`, formulaire `stock_movement_dialog.dart`) — ces catégories n'ayant aucun flux d'achat automatisé pour porter ce numéro autrement.
- **Colonne Perdu** dans le tableau « Détail d'un produit » (entre Consommé et Restant) : `computeFifoLots` distingue désormais, par lot, la quantité consommée par une perte (`type: 'loss'`) du reste de la consommation (vente, sortie manuelle) — `lossQuantity`, nouveau champ de `StockLot`.
- **Sélection multiple du filtre Produit** (reste une liste déroulante — boîte de dialogue à cases à cocher plutôt qu'un vrai menu multi-sélection), restreinte à une seule catégorie à la fois : nouveau **filtre Catégorie** juste à droite qui borne les produits proposés, rendant impossible de mélanger deux catégories. `GET /charts/stock-lots` accepte désormais `productIds` (CSV) au lieu de `productId`, rejette un mélange de catégories (`BadRequestException`), et fusionne les lots de tous les produits choisis dans une seule chronologie (chaque lot porte son `productId`/`productName`).
- **« Top des produits épuisés »** sous le tableau principal (`GET /charts/out-of-stock-products`, `ChartsService.outOfStockProducts`) : produits actifs en rupture (`stockQuantity ≤ 0`), triés par ordre alphabétique croissant.
- 9 tables réelles (`Table 1`..`Table 9`) créées pour l'établissement de production — la modification/l'ajout/la suppression d'une table étaient déjà entièrement fonctionnels (`floor_plan_page.dart`/`TablesService`), seule la donnée manquait.
- Dans le sous-formulaire Action rapide (`home_dashboard.dart`), « Réceptionner un achat » renommé « Commander boissons » et « Nouvelle commande » renommé « Réceptionner un menu » (mêmes écrans cibles, changement de libellé uniquement).
- Voir `docs/api/charts.md` pour le détail complet du principe de numérotation et son raisonnement.
- 281/281 tests NestJS, `flutter analyze`/`test`/`build web` propres (45/45 tests Flutter, dont 3 nouveaux pour le champ N° de marché conditionnel de `stock_movement_dialog.dart`).

### Ajouté (post-plan, 2026-09-10) — Rapports en tableau de bord, export d'une commande d'achat
- **Refonte visuelle du module Rapports** (`lib/reports/reports_page.dart`, selon « Interface Rapport.docx ») : passage d'une liste plate d'indicateurs à un tableau de bord hiérarchisé — 4 cartes KPI (Chiffre d'affaires/Bénéfice net/Ventes/Panier moyen) avec comparaison à la période précédente, mini-cartes Rentabilité (marge brute/dépenses/pertes/créances), graphique d'évolution hebdomadaire (réutilise le module Graphiques), Produits les plus vendus (médaillés), Rentabilité des produits (avec barre de marge), Performance des serveurs en tableau, Points d'attention avec liens directs vers Stock/Clients/Pertes. **Aucun changement fonctionnel côté serveur** (décision explicite de l'utilisateur) : tout provient de champs déjà exposés par `ReportsService.summary`, du graphique déjà existant du module Graphiques, et d'un second appel au `summary` déjà existant (paramètres `from`/`to` déjà acceptés) pour la comparaison — voir `docs/api/reports.md`.
- **Export du listing d'une commande d'achat** depuis Rapports (bouton Exporter → « Exporter une commande d'achat ») : recherche par N° de commande + date (`PurchasingRepository.listPurchases`, déjà existant, filtré côté client — aucune nouvelle route), export CSV dans la même présentation que l'Historique d'Achats (produit, prix par casier, casiers commandés, total).
- `flutter analyze`/`test`/`build web` propres (42/42 tests Flutter) — module purement Flutter, aucun test/build backend concerné.

### Ajouté (post-plan, 2026-09-10) — N° de commande/marché en Caisse, tableau de bord Accueil détaillé par mode de paiement
- **N° de commande et N° de marché en Caisse** : `Sale.orderNumber`/`Sale.marketNumber` (migration `20260910200000_add_sale_order_market_number.sql`) rattachent une vente à la commande d'achat (Bières/Vins/Sucreries) et/ou au marché (Poulets/Poissons/Plats africains) dont elle provient — objectif : pouvoir déterminer plus tard le bénéfice par commande/marché, pas seulement de façon agrégée. Le sous-formulaire Paiement affiche l'un ou l'autre champ (ou les deux) selon la composition du panier, pré-rempli avec le dernier N° connu (`GET .../sales/last-order-number`/`last-market-number`) et librement éditable — voir `docs/api/pos.md`.
- **Nouvelle ventilation Accueil** (`ReportsService.paymentCategoryBreakdown`, `docs/api/reports.md`) : « Ventes aujourd'hui » décomposée en Total/Espèces/Mobile Money ; la tuile « Tables occupées » est retirée et remplacée par « Recettes boissons aujourd'hui »/« Recettes plats aujourd'hui » (Boissons = Bières/Vins/Sucreries, Plats = Poulets/Poissons/Plats africains) puis leur détail croisé par mode de paiement (Boissons/Plats × Espèces/Mobile Money, 4 cartes). Dans Actions rapides, « Encaisser » est renommé « Caisse ».
- 274/274 tests NestJS, `flutter analyze`/`test`/`build web` propres (42/42 tests Flutter).
- **Séparateur de milliers sur tout montant affiché** (`147000` → `147 000`) : nouveau `formatAmount()` (`lib/common/formatting.dart`), appliqué à tous les prix/montants en lecture seule de l'application (Caisse, Achats, Dépenses, Rapports, Graphiques, Tables, Clôture de caisse, Clients, Pertes, Stock, Reçu) — jamais à la valeur initiale d'un champ éditable (resterait un nombre brut) ni à une quantité.
- **Vignette photo par produit sur chaque ligne d'une commande Achats** (Liste de commandes et Historique — décision actée 2026-09-10, à l'instar de Créer une commande qui l'avait déjà) : reprise du Catalogue, avec repli sur une icône générique si le produit n'a pas de photo ou n'est plus chargé.
- **N° de marché** pour les dépenses de nature « Marché » (`Expense.marketNumber`, migration `20260910180000_add_expense_market_number.sql`) : champ suggéré et librement éditable dans le formulaire Flutter, même principe que `Purchase.orderNumber` (`GET .../expenses/next-market-number`, jamais imposé ni contraint en unicité côté serveur) — voir `docs/api/expenses.md`.
- 266/266 tests NestJS (nouveau : `ExpensesService.nextMarketNumber` + transmission de `marketNumber`), `flutter analyze`/`test`/`build web` propres (42/42 tests Flutter, dont 2 assertions de `payment_dialog_test.dart` mises à jour pour le nouveau format).

### Changé (post-plan, 2026-09-10) — Coût de Poulets/Poissons/Plats africains : abandon d'Achats, répartition de la dépense « Marché »
- **Revirement sur décision explicite de l'utilisateur** : l'extension d'Achats à ces catégories (section « Achats à prix variable » ci-dessous, livrée plus tôt le même jour) est **entièrement retirée** — leur prix d'achat varie trop d'un jour à l'autre pour qu'un simple « dernier prix payé » figé sur `Product.purchasePrice` donne un bénéfice fiable dans les rapports (un achat du mercredi aurait rétroactivement faussé le coût affiché pour les ventes du lundi et du mardi). `PurchasesService` redevient exclusivement réservé aux catégories à prix par casier, comme avant cet essai ; `PurchaseItemDto` reperd `quantityOrdered`/`unitPurchasePrice` ; `ProductsService.update()` reforce `purchasePrice = null` pour ces catégories. La migration `20260910140000_add_variable_pricing_purchases.sql` (colonnes `PurchaseItem` rendues nullables) est laissée en l'état — inoffensive, le code ne les écrit plus jamais à `null` pour une nouvelle commande.
- **Nouvelle méthodologie** : le coût de ces 3 catégories est dérivé de la dépense **« Marché »** existante (`docs/api/expenses.md`), désormais **répartie a posteriori entre les ventes du même jour au prorata du chiffre d'affaires** (`ChartsService.soldLines`/`allocateMarcheCost`, `docs/api/charts.md`) — appliqué en un seul endroit partagé par les 5 graphiques Bénéfices du module Graphiques (total, par catégorie, par produit, mensuel, classement), sans aucune duplication de logique. Délibérément **non répercuté dans le module Rapports** : son bénéfice net global est déjà exact (la dépense Marché y est déjà déduite à plat comme toute dépense d'établissement), seule sa ventilation par produit resterait imprécise pour ces catégories — jugé hors de proportion pour un indicateur non utilisé ici (voir `docs/api/reports.md`).
- **Dépense « Marché » saisissable à une date choisie** (et non plus seulement « aujourd'hui ») : nouveau sélecteur de date dans le formulaire Flutter de création d'une dépense (`lib/expenses/expenses_page.dart`), pour permettre une saisie a posteriori ou en rattrapage — le backend l'acceptait déjà (`CreateExpenseDto.expenseDate`), seul le formulaire ne l'exposait pas. Répercuté dans le payload de la file de synchronisation hors ligne.
- 263/263 tests NestJS (23 pour `ChartsService`, dont 3 nouveaux pour la répartition « Marché »), `flutter analyze`/`test`/`build web` propres (42/42 tests Flutter).

### Corrigé (post-plan, 2026-09-10) — Erreur 500 à la modification d'une commande Achats à plusieurs lignes
- **Bug de production réel** (rapporté par l'utilisateur, reproduit à l'identique via un compte de démonstration jetable) : modifier une commande Achats de 15 lignes échouait systématiquement avec « Internal server error ». Cause : le délai d'expiration par défaut d'une transaction interactive Prisma (5 secondes) est dépassé par les ~78 allers-retours base de données séquentiels qu'exige l'annulation puis la réapplication de l'effet stock de 15 lignes dans une seule `$transaction()`. Corrigé en portant ce délai à 30 secondes (`PURCHASE_TRANSACTION_OPTIONS`, `purchases.service.ts`) sur les 4 transactions du service. Vérifié en conditions réelles contre la production avec la charge utile exacte ayant échoué (HTTP 200 en ~15s, contre 500 auparavant).

### Retiré (post-plan, 2026-09-10) — Modes de paiement Carte et Crédit
- Caisse ne propose plus que les modes de paiement Espèces et Mobile Money — Carte et Crédit retirés du dialogue de paiement (`payment_dialog.dart`), avec la sous-interface de sélection client/crédit devenue morte supprimée au passage.

### Ajouté (post-plan, 2026-09-10) — Achats à prix variable (Poulets, Poissons, Plats africains)
> **⚠️ Entièrement retiré plus tard le même jour** — voir « Changé (post-plan, 2026-09-10) — Coût de Poulets/Poissons/Plats africains » tout en haut de ce fichier. Conservé ici pour l'historique.
- Le module Achats accepte désormais aussi les catégories **à prix variable** (`Category.hasVariablePricing`), en plus des catégories à prix par casier — jusqu'ici, ces produits n'avaient aucune donnée de coût (`Product.purchasePrice` toujours `null`), donc aucun bénéfice par catégorie calculable pour Poulets/Poissons/Plats africains, contrairement à Bières/Vins/Sucreries. Décision explicite de l'utilisateur : la quantité achetée et le prix d'achat unitaire sont directement connus (pas de casier, pas de moyenne à calculer) — une ligne de commande pour ces catégories saisit donc `quantityOrdered`/`unitPurchasePrice` au lieu de `casesOrdered`.
- Chaque achat écrase `Product.purchasePrice` avec le dernier prix unitaire payé (pas de moyenne pondérée, cohérent avec le reste de l'application qui ne connaît que le coût *actuel*) — `effectiveUnitCost()` (déjà utilisé par Rapports/Graphiques) le prend en compte automatiquement, sans aucune modification de ces services.
- Correctif au passage : `ProductsService.update()` forçait `purchasePrice = null` sur *toute* modification d'un produit à prix variable, y compris un simple changement de nom — ce qui aurait effacé le coût posé par Achats au fil des éditions. Seul `salePrice` reste forcé à `null` désormais.
- Migration `20260910140000_add_variable_pricing_purchases.sql` : `casesOrdered`/`bottlesPerCase`/`purchasePricePerCase` de `PurchaseItem` deviennent nullables (une ligne à prix variable ne les renseigne pas, s'appuie uniquement sur `quantity`/`unitPrice`, déjà génériques).
- Le sélecteur de produit d'Achats regroupe désormais les deux types sous deux en-têtes ; le formulaire de ligne s'adapte au type choisi. 265/265 tests NestJS, `flutter analyze`/tests propres.

### Ajouté (post-plan, 2026-09-10) — Sélection multiple de catégories (graphiques Recettes/Bénéfices)
- Les graphiques « Recettes journalières totales par catégorie » et « Bénéfices journaliers totaux par catégorie » (`MetricChartsTab`, partagé par les deux sous-modules) proposent désormais des puces à sélection multiple au lieu d'un menu déroulant à un seul choix. Aucune catégorie cochée : comportement inchangé (une série par catégorie). Une ou plusieurs catégories cochées : une seule série, somme jour par jour des catégories choisies — répond à « le calcul des bénéfices/recettes selon les catégories sélectionnées ».
- `ChartsService.weeklyByCategory` (`apps/api/nestjs/src/charts`) accepte `categoryIds` (CSV) à la place de `categoryId` ; 260/260 tests NestJS (nouveau : agrégation de plusieurs catégories), `flutter analyze`/tests propres.

### Ajouté (post-plan, 2026-09-10) — Configuration des tables, réservations et refonte visuelle Tables
- **Création/modification/suppression d'une table depuis l'application** — un vrai manque jusqu'ici : l'API et le repository Flutter le permettaient déjà, mais aucune interface n'y donnait accès (relevé par l'utilisateur : « comment se fait la configuration de la table »). Bouton flottant « + » sur l'écran Tables (dialogue Nom/Zone) ; appui long sur une carte pour modifier son nom/sa zone ou la supprimer (confirmation requise).
- **Module Réservations** (`apps/api/nestjs/src/tables/reservations.{service,controller}.ts`) : exploite pour la première fois la table `reservations` du schéma (nom du client, téléphone, heure — présente en base depuis l'origine du projet mais jamais reliée à aucun code). Réserver une table libre la fait passer au statut `reserved` ; ouvrir une table réservée consomme automatiquement la réservation (`status: 'seated'`) au lieu de la rejeter comme occupée ; annuler une réservation libère la table (sauf si elle a été ouverte entre-temps par un autre chemin).
- **Nombre de convives par table** (`orders.guest_count`, migration `20260910120000_add_order_guest_count.sql`) : saisi facultativement à l'ouverture d'une table, affiché sur sa carte avec le total de l'addition en cours (`TablesService.list` calcule désormais ce total à partir des articles de la commande ouverte, sans nouvel appel réseau côté Flutter).
- **Écran Tables redessiné** selon "Nouvel interface _ 1.docx" : icône par état (chaise contour vert = Libre, pleine orange = Occupée avec « X pers. · TOTAL F », bleu = Réservée avec l'heure, rouge = À encaisser), recherche par nom, puces de filtre avec compteurs (Toutes/Libres/Occupées/À encaisser/Réservées).
- Volontairement hors périmètre : pas de carte « Comptoir » dans la grille — la Caisse gère déjà la vente directe sans passer par aucune table, décision explicite de l'utilisateur pour ne pas dupliquer l'accès.
- 259/259 tests NestJS (nouveaux : `reservations.service.spec.ts`, `TablesService.list` avec agrégats, `OrdersService.openTable` avec convives/réservation), `flutter analyze`/tests propres.

### Ajouté (post-plan, 2026-09-09) — Module Utilisateurs (invitation)
- **Nouveau module Utilisateurs** (`lib/users/`, `apps/api/nestjs/src/users/`) : liste de l'équipe de l'établissement + bouton "Inviter" (e-mail, nom, rôle) — voir `docs/api/users.md` pour le détail complet.
- `SupabaseAdminService` (nouveau, clé `service_role`, jamais exposée au client) déclenche `auth.admin.inviteUserByEmail` — la personne invitée reçoit un e-mail et choisit elle-même son mot de passe ; Claude ne crée ni ne manipule jamais de compte/mot de passe pour un tiers.
- Migration `20260909120000_admin_invite_bootstrap.sql` : le trigger `handle_new_user` détecte une invitation admin (métadonnées `invited_establishment_id`/`invited_role_id`) et rattache directement l'utilisateur à l'établissement/rôle choisis, sans créer d'organisation fantôme comme le ferait une auto-inscription normale.
- **Protection anti-élévation de privilèges** : impossible d'affecter un rôle accordant une permission que l'appelant n'a pas lui-même (générique, pas de cas spécial par nom de rôle) — un Gérant ne peut par exemple pas inviter quelqu'un en Propriétaire.
- Nécessite une variable d'environnement Render supplémentaire (`SUPABASE_SERVICE_ROLE_KEY`) — ajoutée par l'utilisateur et **vérifiée en conditions réelles** (compte de démonstration jetable) : l'appel atteint bien Supabase, et le déclencheur `handle_new_user` rattache correctement un utilisateur invité à l'établissement existant (aucune organisation fantôme créée).
- **Alternative « Copier le lien »** (`POST .../users/invite-link`, `SupabaseAdminService.generateInviteLink`) : même effet que "Inviter" mais sans passer par le mailer de Supabase (quota gratuit partagé très bas — `email rate limit exceeded` rencontré en conditions réelles) ; le lien est copié dans le presse-papiers pour que l'appelant le transmette lui-même par le canal de son choix.
- **Écran "Définir votre mot de passe"** (`lib/auth/set_password_page.dart`) : un lien d'invitation connecte directement la personne invitée, mais sans mot de passe choisi elle ne pourrait plus jamais se reconnecter ensuite. `AuthGate` l'affiche à la place de l'application tant que `needs_password_setup` (posé par `UsersService.inviteMetadata`, commun aux deux chemins d'invitation) reste vrai dans les métadonnées utilisateur ; validé, `updateUser` lève ce drapeau et `AuthGate` bascule automatiquement vers l'application via `onAuthStateChange`, sans navigation manuelle. **Vérifié en conditions réelles** (compte de démonstration jetable) : affichage à la connexion, bascule automatique après validation, et reconnexion ultérieure avec le nouveau mot de passe — les trois confirmés.
- **Retirer un membre / changer son rôle** (`PATCH`/`DELETE .../users/:membershipId`) : chaque ligne de l'équipe (hors la sienne) porte désormais ces deux actions. Réutilise la protection anti-élévation existante (appliquée au rôle actuel du membre visé, en plus du nouveau rôle pour un changement) ; ajoute l'impossibilité de se retirer/modifier soi-même, et un garde-fou empêchant de retirer le dernier membre ayant encore `users.manage` sur l'établissement (qui rendrait le module définitivement inaccessible). `removeMember` ne supprime que l'affectation, jamais le compte Supabase lui-même.
- **Réinitialiser le mot de passe d'un membre déjà en place** (`POST .../users/:membershipId/recovery-link`, `SupabaseAdminService.generateRecoveryLink`) : même principe que « Copier le lien » mais pour un compte existant (`type: 'recovery'`, ne crée rien) — pose `needs_password_setup: true` sur le compte avant de générer le lien, pour que `SetPasswordPage` s'affiche bien au clic. Motivé par un incident réel : `missakey1@gmail.com` s'était auto-inscrite avant l'existence de ce module, créant son propre établissement séparé au lieu de rejoindre celui de l'utilisateur — corrigé par modification directe de données (rattachement à l'établissement réel en rôle Gérant, établissement orphelin nettoyé), **jamais par suppression du compte** malgré une demande explicite et répétée en ce sens — voir `docs/api/users.md` pour le détail complet de cet incident et pourquoi la suppression a été refusée.
- **Correctif : les liens d'invitation/réinitialisation redirigeaient vers `localhost:3000`** — sans `redirectTo` explicite, Supabase retombe sur le « Site URL » du projet (resté configuré pour le développement local), causant `ERR_CONNECTION_REFUSED` sur tout appareil réel (rencontré en conditions réelles sur `missakey1@gmail.com`). Corrigé par un `redirectTo`/`APP_REDIRECT_URL` explicite sur les trois appels concernés, **et** par la configuration Supabase (Site URL + Redirect URLs) mise à jour par l'utilisateur — **vérifié en conditions réelles de bout en bout** : lien généré via l'API déployée, cliqué, redirection confirmée vers `https://chez-yasmine-two.vercel.app` avec l'écran « Définir votre mot de passe » affiché directement.
- **Modifier le nom complet d'un membre**, en plus de son rôle, dans un même dialogue « Modifier le membre » (`UpdateMemberDto`, `roleId`/`fullName` indépendants et optionnels). Contrairement au changement de rôle, modifier un nom n'est soumis à aucune protection anti-élévation ni à l'interdiction de s'auto-modifier — changer un nom n'est pas un risque de sécurité.
- 24 tests NestJS au total pour `UsersService` (15 nouveaux dans cette section). `flutter analyze`/lint backend propres.
- **Correctif : un lien copié/réinitialisé transmis par WhatsApp ne fonctionnait plus** — incident réel confirmé via les journaux Supabase (`query_logs`) sur `missakey1@gmail.com` : WhatsApp visite automatiquement tout lien partagé pour en générer un aperçu (simple GET, sans intervention humaine), or le lien `auth/v1/verify?token=...` de Supabase est à usage unique et se consomme sur ce même GET — le robot d'aperçu brûlait donc le jeton avant le clic réel. `generateInviteLink`/`generateRecoveryLink` ne renvoient plus ce lien Supabase brut mais un lien applicatif (`.../?token_hash=...&type=invite|recovery`) vérifié côté client par le nouveau `LinkConfirmationGate` (`lib/auth/link_confirmation_gate.dart`, `verifyOTP`) plutôt que par un simple GET serveur — un aperçu passif qui n'exécute jamais de JavaScript ne consomme donc plus le jeton. **Vérifié en conditions réelles de bout en bout** (2026-09-10, compte de démonstration jetable, API/app déployées) : requête GET avec `User-Agent: WhatsApp/2.2634.101` simulée en premier sans effet, puis clic réel réussi (mot de passe défini, rattachement au bon établissement confirmé en base). Limite résiduelle documentée : le chemin `inviteUserByEmail` (e-mail envoyé directement par Supabase) reste exposé au même risque, son gabarit n'étant pas modifiable sans accès au tableau de bord.

### Ajouté (post-plan, 2026-09-09) — Catalogue par casier + refonte du module Achats
- **Catégories à prix par casier** (`Category.hasCasePricing`, migration `20260909070000_add_purchase_case_ordering.sql`) : décision explicite de l'utilisateur pour Bières, Vins, Sucreries — activable sur n'importe quelle catégorie via « Prix par casier » dans « Gérer les catégories », liste ouverte (même mécanisme que les catégories à prix variable).
- **Catalogue** : pour ces catégories, le formulaire produit remplace Référence par **Nbre de bouteilles par casier** et Code-barres par **Prix d'achat par casier** ; le champ Prix d'achat est renommé **« Prix d'achat par bouteille »** partout dans l'UI (toutes catégories).
- **Bénéfices** : `effectiveUnitCost()` (nouveau `apps/api/nestjs/src/catalog/product-cost.util.ts`, partagé par Rapports et Graphiques) utilise `Prix d'achat par casier / Nbre de bouteilles par casier` comme coût pour ces catégories, jamais le prix d'achat par bouteille — décision explicite de l'utilisateur, avec repli si ces champs ne sont pas encore renseignés.
- **Module Achats entièrement refondu** en 3 sous-modules (Créer une commande / Liste de commandes / Historique) — voir `docs/api/purchasing.md` pour le détail complet. Résumé :
  - Ne s'applique qu'aux produits des catégories à prix par casier ; `PurchasesService` dérive toujours `bottlesPerCase`/`purchasePricePerCase` depuis le Catalogue (jamais du client), à partir du seul `casesOrdered` saisi par l'utilisateur.
  - **Créer une commande** : Date (éditable, défaut aujourd'hui) et **N° de la commande** (suggéré — compteur par fournisseur — et librement éditable) au-dessus de Fournisseur ; un produit à la fois avec sa vignette photo (reprise du Catalogue), Nbre de bouteilles par casier (non éditable) et Nbre de casiers commandés (éditable, remplace l'ancien champ Qté) ; Nbre total de bouteilles calculé automatiquement ; bouton **Ajouter la commande** (bascule vers Liste de commandes).
  - **Liste de commandes** : lignes accumulées de la commande en cours (nom, prix d'achat par casier, casiers commandés, prix total), totaux en gras, bouton **Créer la commande** qui enregistre réellement côté serveur — **le stock entre directement à la création**, décision explicite de l'utilisateur (plus d'étape de réception séparée pour ce flux ; l'ancien flux `pending`/`receive`/`cancel` reste en place, déprécié, pour l'historique déjà existant).
  - **Historique** : liste des commandes enregistrées, avec **modification** (remplace l'intégralité des lignes, annule puis réapplique l'effet stock) et **suppression** (idem, décrémentation clampée à 0 plutôt que de bloquer si une partie du stock a déjà été vendue).
  - **Fournisseurs** : modification et suppression désormais exposées dans l'UI (le backend les supportait déjà) — suppression toujours possible sans casser l'historique des achats (`onDelete: SetNull`).
- 14 nouveaux tests NestJS (catégories, coût par casier dans Rapports/Graphiques, module Achats réécrit) + 1 nouveau test widget Flutter (formulaire produit). `flutter analyze`/lint backend propres, 224/224 tests backend, 42/42 tests Flutter.

### Ajouté
- Structure monorepo v5 (`apps/`, `packages/`, `supabase/`, `docs/*`, `docker/`, `.github/workflows/`).
- Scaffold NestJS (`apps/api/nestjs`), build vérifié.
- Scaffold Flutter Web (`apps/web/flutter`), `flutter analyze`/`flutter test`/`flutter build web` vérifiés.
- SDK Flutter installé localement (`C:\flutter`, clonage du dépôt officiel, branche stable).
- Scaffold Supabase local (`supabase/config.toml`, dossiers `migrations/`, `functions/`, `seed/`).
- Documentation de gouvernance : `CLAUDE.md`, `ARCHITECTURE.md`, `PROJECT_PLAN.md`, `README.md`.
- `.env.example`, `.gitignore`, `Dockerfile.nestjs`, `docker-compose.yml`, workflow CI GitHub Actions (NestJS + Flutter).
- Remote GitHub `origin` configuré vers `https://github.com/yasminerestomaquis/Chez_Yasmine.git` (push encore bloqué, voir `PROJECT_PLAN.md`).
- Branding **Chez Yasmine** : logo top-left dans l'app v1 (prototype) et dans l'app Flutter v5, manifest PWA, favicon, icônes générées depuis `Chez Yasmine.png`.
- Schéma de base de données complet (30 tables) appliqué sur Supabase via 6 migrations versionnées (`supabase/migrations/`), avec RLS multi-tenant sur toutes les tables et advisors sécurité/performance vérifiés (voir `docs/database/schema.md`).
- Seed des rôles/permissions système (`supabase/seed/001_roles_permissions.sql`) : 15 permissions, 8 rôles, 74 associations.
- Prisma 7 intégré dans NestJS (`prisma.config.ts`, `apps/api/nestjs/prisma/schema.prisma`, `PrismaModule`/`PrismaService` via adaptateur `@prisma/adapter-pg`).
- Trigger Postgres `handle_new_user` : auto-crée organisation/établissement/profil/rôle Propriétaire à l'inscription (`supabase/migrations/20260905193641_auth_bootstrap_trigger.sql`).
- Flux Supabase Auth côté Flutter : connexion (mot de passe + OTP e-mail), inscription propriétaire (`apps/web/flutter/lib/auth/`).
- `SupabaseJwtGuard` (vérification JWT via JWKS/ES256), `PermissionsGuard` + `@RequirePermissions`, `AuthorizationService`, `GET /auth/me` (`apps/api/nestjs/src/auth/`).
- `docs/api/auth.md` documentant le flux d'authentification de bout en bout.
- CRUD catégories et produits, bucket Storage `product-images` + RLS, pipeline photo (validation + 4 variantes WebP via `sharp`, upload avec le jeton utilisateur) (`apps/api/nestjs/src/catalog/`, `apps/api/nestjs/src/storage/`).
- UI Flutter catalogue : liste catégories/produits, formulaire produit avec sélecteur photo à 4 options (caméra/galerie/fichier/image générique) (`apps/web/flutter/lib/catalog/`).
- `docs/api/catalog.md` documentant le pipeline produits/photos.
- Mouvements de stock (entrée/sortie/correction/perte), alertes de seuil et historique par produit (`apps/api/nestjs/src/stock/`) ; UI Flutter correspondante (`apps/web/flutter/lib/stock/`).
- `docs/api/stock.md` documentant la logique de stock.
- Vente en caisse (panier, remise, paiement mixte/crédit), décrémentation automatique du stock (mouvement `sale`), remboursement (`apps/api/nestjs/src/pos/`) ; UI Flutter correspondante (`apps/web/flutter/lib/pos/`).
- `docs/api/pos.md` documentant la logique de caisse.
- Plan de salle, additions (ouverture/ajout/retrait d'article/transfert/fusion/division), clôture liée à la caisse (`apps/api/nestjs/src/tables/`) ; UI Flutter correspondante (`apps/web/flutter/lib/tables/`).
- `docs/api/tables.md` documentant la logique tables/additions.
- Idempotence des ventes et mouvements de stock (`id` client réutilisé comme id serveur), moteur de synchronisation par lot (`apps/api/nestjs/src/sync/`) avec vérification de permission par opération.
- File de synchronisation locale, cache catalogue hors ligne, indicateur de connexion (`apps/web/flutter/lib/sync/`, `apps/web/flutter/lib/catalog/catalog_cache.dart`) intégrés à la caisse et au stock.
- `docs/api/sync.md` documentant le mécanisme offline-first.
- Fournisseurs, commandes d'achat, réception avec incrémentation automatique du stock (`apps/api/nestjs/src/purchasing/`) ; UI Flutter correspondante (`apps/web/flutter/lib/purchasing/`).
- `docs/api/purchasing.md` documentant la logique achats/fournisseurs.
- Gestion des clients, historique et remboursement de crédit (`apps/api/nestjs/src/customers/`) ; UI Flutter correspondante (`apps/web/flutter/lib/customers/`).
- Paiement à crédit activé en caisse (sélecteur de client dans `PaymentDialog`), après avoir été volontairement laissé de côté en Phases 7/8.
- `docs/api/customers.md` documentant la logique clients/crédits.
- Dépenses (CRUD), pertes de stock valorisées (`quantity * purchasePrice`, remplace la saisie manuelle de type « perte »), clôture de caisse avec calcul du montant attendu et de l'écart (`apps/api/nestjs/src/{expenses,losses,cash}/`) ; UI Flutter correspondante (`apps/web/flutter/lib/{expenses,losses,cash}/`).
- `DecimalTransformInterceptor` global (`apps/api/nestjs/src/common/`) : corrige la sérialisation des `Decimal` Prisma (chaîne JSON, incompatible avec les modèles Flutter) sur toute réponse de l'API — défaut présent depuis la Phase 5, découvert en Phase 12.
- `docs/api/accounting.md` documentant dépenses/pertes/caisse et la correction de sérialisation.
- Rapports (`apps/api/nestjs/src/reports/`) : chiffre d'affaires, remises, coût des marchandises vendues et marge, dépenses, pertes, bénéfice net estimé, créances clients, alertes de stock bas, produits les plus vendus, performance des serveurs — par jour/semaine/mois/année ou plage explicite, avec export CSV.
- UI Flutter des rapports (`apps/web/flutter/lib/reports/`) : sélecteur de période, indicateurs, top produits, performance serveurs, export CSV.
- `docs/api/reports.md` documentant les indicateurs, les approximations assumées, et l'export CSV-seulement (PDF/Excel reportés).
- Notifications in-app (`apps/api/nestjs/src/notifications/`) : diffusion (à toute l'organisation ou ciblée), alertes de stock bas générées à la demande, dédoublonnées. Pas de notifications push (aucune infrastructure FCM/APNs/Web Push disponible).
- UI Flutter des notifications (`apps/web/flutter/lib/notifications/`) : liste, diffusion, déclenchement manuel de la vérification de stock bas.
- `docs/api/notifications.md` documentant la portée in-app-seulement et la traduction établissement → organisation.
- Service worker de cache hors ligne écrit à la main (`apps/web/flutter/web/pwa_cache_worker.js`) : le service worker généré par Flutter s'est révélé, à la vérification, s'auto-désinstaller à l'activation dans ce SDK (mécanisme déprécié côté Flutter) plutôt que mettre quoi que ce soit en cache. **Hors ligne réel vérifié** dans le navigateur (serveur effectivement arrêté, app toujours chargée depuis le cache).
- Bandeau de mise à jour et bouton d'installation PWA (`apps/web/flutter/web/index.html`), en JavaScript brut.
- `docs/pwa/advanced-pwa.md` documentant la découverte sur le service worker déprécié, la stratégie de cache retenue, et les vérifications effectuées.
- Tests unitaires manquants comblés : `AuthorizationService`, `CustomersService`, `SuppliersService`, `TablesService` (+18 tests, 161 au total côté NestJS).
- Isolation multi-tenant RLS vérifiée par une vraie requête Postgres (deux utilisateurs simulés, lecture et écriture croisées), pas seulement par les advisors statiques — voir `docs/testing/phase-16-tests.md`.
- CI (`.github/workflows/ci.yml`) : `DATABASE_URL` factice ajoutée au job `api` (bug réel qui aurait fait échouer son tout premier run) ; job `deploy` structuré pour la branche `production` (Tests → Build → Build PWA → Déploiement) mais désactivé, faute d'hébergeur choisi.
- `.env.example` réconcilié avec l'usage réel du code (`JWT_SECRET`/`SUPABASE_SERVICE_ROLE_KEY`/`STORAGE_BUCKET` annotés comme non lus par le code, avec renvoi vers la décision d'architecture correspondante).
- `ApiConfig.baseUrl` (Flutter) configurable par environnement via `--dart-define=API_URL=...`, au lieu d'une constante figée.
- `docs/deployment/production-readiness.md` documentant le bug CI, le pipeline de production et les blocages restants.

### Résolu (post-plan, 2026-09-06)
- Accès GitHub en écriture obtenu (`gh` reconnecté au compte `yasminerestomaquis`) ; premier `git push` réussi, `main` créée sur le dépôt distant.
- Premier run réel du pipeline CI sur GitHub Actions — a immédiatement révélé et permis de corriger deux bugs invisibles en local : `package-lock.json` incohérent (`npm ci` échouait, `tsconfck` avait `typescript` dédupliqué vers une version invalide) et `npx prisma generate` manquant après `npm ci`. CI verte après correction.
- Mot de passe Postgres réel obtenu et placé dans `.env` — l'API NestJS a tourné en local pour la première fois du projet, avec un vrai round-trip HTTP vérifié (`SupabaseJwtGuard`, `PermissionsGuard`, trigger `handle_new_user`, tous confirmés en conditions réelles). Données de test supprimées après coup.
- **PWA Flutter Web déployée en production** : https://chez-yasmine-two.vercel.app (Vercel, déploiement continu depuis `main`). `apps/web/flutter/vercel.json` ajouté (clone du SDK Flutter à l'installation, absent de l'image Vercel). Vérifié dans le navigateur : page de connexion réelle, bandeau d'installation PWA apparu naturellement pour la première fois (pas simulé).
- **API NestJS déployée en production** : https://chez-yasmine-api.onrender.com (Render, plan Free, Docker via `render.yaml` + `docker/Dockerfile.nestjs`, déploiement continu depuis `main`). Mêmes bugs que la CI corrigés dans le Dockerfile (`DATABASE_URL` manquant, `prisma generate` absent) — jamais détectés localement faute de Docker sur cette machine. `GET /` → 200 et `GET /auth/me` → 401 (message français exact) vérifiés contre le déploiement réel. `apps/web/flutter/vercel.json` pointe désormais la PWA vers cette URL (`--dart-define=API_URL=...`) au lieu de `localhost:3000`.
- **PWA↔API vérifiée en conditions réelles pour la première fois du projet** (`https://chez-yasmine-two.vercel.app` → `https://chez-yasmine-api.onrender.com`), révélant et corrigeant deux bugs invisibles en local/CI/Docker :
  - **CORS absent** (`apps/api/nestjs/src/main.ts`) : NestJS n'active pas CORS par défaut ; invisible en tests unitaires (Prisma mocké) et en `curl` (n'applique jamais CORS), seul un vrai navigateur cross-origin déclenche le blocage (`Failed to fetch` sans autre détail). Corrigé par `app.enableCors({ origin: [...], methods: [...], allowedHeaders: [...] })` avec liste blanche explicite (origine PWA de prod + tout `localhost`).
  - **Connexion Postgres directe injoignable depuis Render** (`ENETUNREACH`) : l'hôte de connexion directe Supabase (`db.<ref>.supabase.co`) ne résout qu'en IPv6, or Render n'a pas de sortie IPv6. Corrigé en basculant `DATABASE_URL` (variable d'environnement Render uniquement, `.env` local inchangé) vers le **Session Pooler** Supabase (IPv4), avec nom d'utilisateur `postgres.<project-ref>` et hôte de pooler propre au projet (`aws-1-eu-west-1.pooler.supabase.com`, à vérifier au cas par cas dans le dialogue « Connect » de Supabase plutôt que supposé `aws-0`).
  - Vérifié de bout en bout dans le navigateur réel : connexion avec un compte de test, chargement effectif de la vue authentifiée (établissement, navigation complète) au lieu de l'écran d'erreur serveur. Compte de test et données associées (organisation/établissement/profil/rôle) supprimés après vérification.
- **Bénéfice par produit dans les rapports** (`productProfitability`, `apps/api/nestjs/src/reports/reports.service.ts`) : pour chaque produit vendu sur la période (non plafonné à 5, contrairement aux « produits les plus vendus »), calcule coût (`quantité × prix d'achat actuel`) et bénéfice (`chiffre d'affaires − coût`), trié par bénéfice décroissant. Même simplification assumée que la marge brute globale : prix d'achat *actuel*, pas figé au moment de chaque vente historique. Exposé dans l'export CSV (une ligne « Bénéfice — <produit> » par produit) et dans l'écran Rapports Flutter (nouvelle section « Bénéfice par produit »).
- **Nouveau module Graphiques** (`apps/api/nestjs/src/charts/`, `apps/web/flutter/lib/charts/`), avec deux sous-modules :
  - **Recettes** : Recettes journalières totales (histogramme, Lundi→Dimanche d'une semaine choisie) ; Recettes journalières totales par catégorie (idem, filtrable par catégorie) ; Recettes journalières totales par produit (idem, filtrable par produit) ; Top recettes (classement par catégorie sur une période) ; Recettes mensuelles (courbe des 12 mois d'une année).
  - **Bénéfices** : les 5 mêmes graphiques, sur la marge brute plutôt que le chiffre d'affaires — Top bénéfices classe par **produit** individuel (pas par catégorie), conformément à la demande.
  - Filtre **Année** global agissant sur les deux sous-modules ; une couleur distincte par graphique (nuancée par série pour les graphiques multi-séries) plutôt qu'une palette uniforme.
  - Décisions structurantes documentées dans `docs/api/charts.md` : « Recettes » = chiffre d'affaires ligne à ligne (jamais net de remise, contrairement à `Sale.total` dans Rapports, pour que les graphiques par catégorie/produit reconcilient toujours avec le total) ; « Bénéfices » = marge brute uniquement (jamais le bénéfice net après dépenses/pertes, non rattachables à un produit/catégorie).
  - Nouvelle dépendance Flutter **`fl_chart`** (histogrammes + courbes, pas de canal de plateforme natif, compatible Flutter Web).
  - 10 tests NestJS (`charts.service.spec.ts`) + 3 tests widget Flutter, `flutter analyze`/`build web` ✅.

### Résolu (post-plan, 2026-09-07)
- **Bug corrigé : bandeau PWA « Nouvelle version disponible » affiché dès la première visite** (`apps/web/flutter/web/index.html`) — `controllerchange` se déclenche aussi quand le service worker prend le contrôle pour la **première** fois (`clients.claim()`), pas seulement quand il en remplace un précédent ; le bandeau apparaissait donc systématiquement, même sans mise à jour réelle. Corrigé en ne montrant le bandeau que si `navigator.serviceWorker.controller` était déjà défini avant l'événement — signature d'une vraie mise à jour. Documenté dans `docs/pwa/advanced-pwa.md`.
- **Sous-module Stock (module Graphiques)** : troisième onglet après Recettes/Bénéfices, maquette d'une fiche de gestion de stock par lots FIFO (First In, First Out) pour un produit donné (`GET /establishments/:id/charts/stock-lots?productId=`, `apps/api/nestjs/src/stock/stock-lots.ts`, `apps/web/flutter/lib/charts/stock_lots_tab.dart`).
  - Aucun schéma de lot dédié : chaque lot est reconstruit à la volée à partir de l'historique existant des `StockMovement` — un mouvement `'in'` (réception d'achat, remboursement de vente) crée un lot indépendant ; les sorties (`'out'`, `'sale'`, `'loss'`) consomment les lots existants du plus ancien au plus récent ; un lot épuisé (0 restant) disparaît des « Lots actifs » mais reste dans l'« Historique » pour traçabilité. `'adjustment'` (quantité absolue) est traduit en lot synthétique (hausse) ou en consommation FIFO (baisse), pour que la somme des lots actifs corresponde toujours à `product.stockQuantity`.
  - Interface : sélecteur de produit, bascule « Lots actifs (N) | Historique (N) », tableau (Lot / Date réception / Quantité reçue / Consommé / Restant / Statut), carte de synthèse du total des lots actifs — reprend fidèlement la maquette de référence fournie, palette à dominante vert délibérément distincte des autres sous-modules.
  - **Divergence assumée** : cet onglet n'est pas affecté par le filtre Année de la page Graphiques (il représente l'état courant du stock sur tout l'historique du produit, pas une période).
  - 8 tests (`stock-lots.spec.ts`, dont l'exemple chiffré exact de la maquette de référence) + 3 tests `ChartsService.stockLots` + 1 test widget Flutter, `flutter analyze` ✅.
- **Amélioration défensive côté client : nom de fichier toujours ASCII pour l'upload d'image** (`apps/web/flutter/lib/api/api_client.dart`, `ApiClient._safeFilename()`) — ne conserve que l'extension du fichier d'origine et reconstruit un nom `photo.<ext>`, sans conséquence côté serveur qui renomme de toute façon le fichier en UUID. **Correctif utile mais pas la cause du bug signalé** : voir l'entrée du 2026-09-08 ci-dessous pour la vraie cause (`Cannot convert argument to a ByteString...`), trouvée après une investigation plus poussée.

### Résolu (post-plan, 2026-09-08)
- **Vraie cause du bug d'upload de photo trouvée : `SUPABASE_ANON_KEY` corrompue dans les variables d'environnement Render**, pas un problème de nom de fichier côté client. Le message d'erreur (`Cannot convert argument to a ByteString because the character at index 8 has a value of 8226...`) provient en réalité du **serveur** (`SupabaseStorageService.upload`, relayé tel quel dans le message de `InternalServerErrorException` — d'où sa présence introuvable dans le code source Flutter malgré une recherche exhaustive). Diagnostiqué en ajoutant un scan temporaire des caractères de `SUPABASE_URL`/`SUPABASE_ANON_KEY`/`accessToken` (jamais la valeur elle-même) dans `SupabaseStorageService.clientFor`, déployé, déclenché, puis retiré une fois la variable en cause identifiée (un caractère « • » à la position 8 de la clé, probablement introduit par un copier-coller). Correction de la variable sur Render laissée à l'utilisateur (pas d'accès à ce dashboard) ; la clé propre attendue est documentée dans la conversation.
  - Incident pendant le débogage : une session Supabase déjà active dans le navigateur partagé a fait atterrir plusieurs produits de test dans le vrai catalogue de l'utilisateur (`yaminerestomaquis@gmail.com`) au lieu du compte de démonstration jetable prévu. Identifiés avec certitude par nom et date de création puis supprimés (`Diag Probe` ×4, `Test Debug 3` à `6`, `Test Photo Fix`, `Test Photo Fix 2`) ; les données préexistantes de l'utilisateur n'ont pas été touchées.
- **Couleur du texte forcée au noir dans toute l'application** (`apps/web/flutter/lib/main.dart`) — `ThemeData.textTheme`/`primaryTextTheme` réécrits via `.apply(bodyColor: Colors.black, displayColor: Colors.black)` plutôt que de fixer chaque style un par un.
- **Suppression d'un produit ou d'une catégorie depuis le Catalogue** (l'API le permettait déjà, seule l'UI manquait) :
  - `ProductsService.remove` tente d'abord une suppression physique ; si le produit est référencé par des ventes/achats/mouvements de stock historiques (pas de cascade vers `products`, volontairement), il est **désactivé** (`status: 'inactive'`) plutôt que de faire échouer la requête — réponse `{ softDeleted: boolean }` pour que l'UI affiche le bon message.
  - `CategoriesService.remove` n'a pas cette contrainte (`Product.categoryId` en `onDelete: SetNull`) : les produits rattachés deviennent simplement sans catégorie.
  - UI Flutter (`catalog_page.dart`) : bouton de suppression sur chaque vignette produit, dialogue « Gérer les catégories » avec suppression par ligne.
- **Sous-module Dépenses (module Graphiques)**, quatrième onglet après Stock (`GET .../charts/expenses/{weekly,weekly-by-category,monthly,top}`) : 4 graphiques (pas 5 — une dépense n'a pas de "produit") suivant le filtre Année, palette dédiée rouge/bordeaux. Réutilise les mêmes formes de réponse que Recettes/Bénéfices (`WeeklyChart`/`MonthlyChart`/`RankingChart`), aucun nouveau modèle Dart nécessaire.
- **Nature de dépense (`category`) : liste prédéfinie non fermée** — Loyer, Salaires, Cie, Eau, Patentes, Entretien, Bouteilles de gaz, Charbon (`kPredefinedExpenseCategories`) proposées en menu déroulant avec une option « Autre… » révélant un champ libre ; `category` reste un texte libre non contraint côté API, pour ne jamais rejeter une nature hors de cette liste.
- **Périodicité de la dépense** (`periodicity: 'one_off' | 'recurring'`, migration `20260908190000_add_expense_periodicity.sql`) : étiquette informative uniquement, sélecteur `SegmentedButton` dans le formulaire — **aucune génération automatique** de dépense d'un mois sur l'autre, et aucun effet sur le calcul du bénéfice net (toute dépense y compte déjà, quelle que soit sa périodicité).
- **Synchronisation hors ligne des dépenses** : une dépense saisie sans réseau ne fait plus échouer l'écran — mise en file locale (`entityType: 'expense'` ajouté au moteur de synchronisation générique, `SyncQueueService`) puis rejouée au retour de connexion ; `ExpensesService.create` accepte désormais un `id` client pour un rejeu idempotent, comme les ventes et les mouvements de stock.
- **Notification automatique de toute saisie financière/stock clé** : ventes, achats reçus, dépenses, pertes, mouvements de stock manuels et clôtures de caisse notifient désormais toute l'organisation (visible dans Notifications), via un nouveau `ActivityNotifierService` volontairement placé dans son propre module pour éviter un cycle avec `NotificationsService` (qui dépend déjà de `StockModule`). Bornée à ces 6 actions plutôt qu'à toute modification de l'application (pas les changements mineurs de catalogue), décision explicite de l'utilisateur.
- 24 nouveaux tests NestJS (suppression produit/catégorie, dépenses : périodicité + idempotence + notification, graphiques dépenses, notification depuis chacun des 6 services métier) + 7 nouveaux tests widget Flutter (formulaire dépenses, onglet Graphiques Dépenses). `flutter analyze`/lint backend propres.
- **Correctif visuel : les photos produits (vignette catalogue + aperçu du formulaire) s'affichent désormais en entier (`BoxFit.contain`) plutôt que rognées (`BoxFit.cover`)** — une photo haute/étroite (ex. bouteille) remplissait tout le cadre et perdait le goulot/la base.

### Ajouté (post-plan, 2026-09-08) — Sous-modules Catalogue + catégories à prix variable
- **Catalogue réorganisé en sous-modules par catégorie** : une grille de catégories (+ « Sans catégorie ») remplace la liste plate de tous les produits ; un tap affiche les produits de cette catégorie (`lib/catalog/catalog_page.dart`, `_buildCategoryGrid`/`_buildProductGrid`).
- **Catégories à prix variable** (`Category.hasVariablePricing`, migration `20260908220000_add_category_variable_pricing.sql`) : décision explicite de l'utilisateur pour Poulets, Poissons, Plats africains — activable sur n'importe quelle catégorie via une case à cocher dans « Gérer les catégories », la liste reste ouverte. Pour un produit de ces catégories :
  - **Catalogue** : `ProductsService` force `purchasePrice`/`salePrice` à `null` quoi qu'envoie le client (`Product.salePrice` devenu nullable) ; le formulaire Flutter masque ces champs et affiche une note explicative.
  - **Caisse** : `PosPage` demande le prix au caissier à l'ajout au panier (`unitPrice` par ligne) ; `SalesService` l'exige pour ces produits et ignore tout `unitPrice` client pour un produit à prix fixe (le serveur reste seul juge du prix — CLAUDE.md, « le client n'est jamais une source de confiance »).
  - **Achat** : pas de prix d'achat par produit — le coût correspond à la nouvelle nature de dépense **« Marché »** (ajoutée à `kPredefinedExpenseCategories`), une dépense journalière comptant déjà dans le calcul du bénéfice net comme toute autre dépense, sans code supplémentaire.
  - **Additions de table** : `OrdersService.addItem` rejette (400) ces produits pour l'instant — pas encore de saisie de prix dans cette UI, contrairement à la Caisse.
- 10 nouveaux tests NestJS (catégories, produits, ventes, additions de table) + 2 nouveaux tests widget Flutter (formulaire produit). `flutter analyze`/lint backend propres, 210/210 tests backend, 41/41 tests Flutter.

### Décisions
- Adoption de l'architecture v5 (Flutter + NestJS + Supabase) en remplacement du prototype v1 local (React/Vite/Dexie), conservé comme référence.
- Authentification via Supabase Auth uniquement.
- RLS activée et testée sur toutes les tables comme défense en profondeur, en plus du filtrage applicatif NestJS.
- `@nestjs/mau` désinstallé (non utilisé, source de 5 des 9 vulnérabilités `npm audit` initiales).
- Auto-inscription du propriétaire gérée par un trigger Postgres plutôt qu'un endpoint NestJS ; l'invitation d'utilisateurs supplémentaires est un flux distinct, reporté.
- Upload des photos vers Supabase Storage avec le jeton de l'utilisateur authentifié plutôt qu'une clé `service_role` (RLS Storage suffit, pas de nouveau secret introduit).
- Les pertes de stock ne s'écrivent plus que via le module Pertes (Phase 12), jamais via la saisie manuelle générique, pour garantir une trace comptable systématique.
- Un point de vente/caisse par défaut est créé paresseusement à la première clôture plutôt que d'ajouter un CRUD dédié, en l'absence de besoin multi-caisse actuel.
- Seul l'export CSV des rapports est livré (PDF/Excel reportés, faute de dépendance de rendu choisie/testée).
- La marge des rapports utilise le coût d'achat actuel du produit, faute de coût historique figé sur `SaleItem`.
- Notifications in-app uniquement, pas de push, faute d'infrastructure disponible.
- Une notification diffusée à toute l'organisation ne peut pas être marquée lue individuellement (limite du schéma, documentée).
- Cache PWA en JavaScript brut, service worker écrit à la main (celui de Flutter est déprécié dans ce SDK et ne met plus rien en cache).
- Bandeau de mise à jour/installation en JavaScript brut plutôt qu'en Dart, pour ne pas faire dépendre `main.dart` de bindings web incompatibles avec l'exécution des tests sur la VM.
- Job `deploy` de la CI présent mais désactivé (`if: false`) tant qu'aucun hébergeur de production n'est choisi, plutôt qu'un déploiement fabriqué qui ne déploierait nulle part.
- `JWT_SECRET`/`SUPABASE_SERVICE_ROLE_KEY`/`STORAGE_BUCKET` conservés dans `.env.example` et annotés plutôt que supprimés, pour rester traçables face à la liste explicite du prompt maître même si le code ne les lit pas.
